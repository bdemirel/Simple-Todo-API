import express from 'express';

export const index = (req : express.Request, res : express.Response) => res.json({});

export const show = () => {};

export const store = () => {};

export const update = () => {};

export const destroy = () => {};
